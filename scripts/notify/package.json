{
  "name": "juno-notify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Daily notifier for Juno — reads Firestore, sends FCM push for the app's cycle triggers.",
  "dependencies": {
    "firebase-admin": "^12.7.0"
  }
}
