# Juno — Backlog (short version)

`ROADMAP.md` is the detailed continuity guide. This is the quick list.

**Three pillars (all from v0.1):** predict next period · forecast cycle-linked mood · conservative
"can / cannot" fertility window. Default mode = **Avoid**. Web app / PWA on GitHub Pages, Firebase sync.

## Now
- [ ] **v0.0 (done):** online home — roadmap + placeholder PWA shell live on Pages.
- [ ] **Firebase setup** — project, Firestore, Email/Password auth, two accounts, household doc, rules.
- [ ] **v0.1 core** — setup flow, log period + daily mood, calendar with predictions, can/cannot
      window (Avoid default + disclaimer), mode toggle, Firebase sync, installable PWA.

## Next
- [ ] **v0.2** — optional morning temperature → confirm ovulation → tighten "can again" + predictions.
- [ ] **v0.3** — mood/PMS forecast surfaced.
- [ ] **v0.4** — notifications (period-soon, entering/leaving "cannot", late alert).
- [ ] **v0.5** — stats (regularity, averages) + "today" phase ring.

## Later / optional
- [ ] Pill / birth-control reminders · partner-lite view · data export · PIN/biometric lock.

## Rule
Every session ends by updating `ROADMAP.md` and pushing. The repo is the only memory.
